This project's canonical agent instructions live in `AGENTS.md` at the
repository root. Before working in this repo, read `AGENTS.md` and follow the
project overview, tech stack, commands (`npm run lint` and `npm run type-check`
must pass), structure, conventions, environment, and agent rules documented
there.
